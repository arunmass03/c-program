#include<stdio.h>
#include<conio.h>
#include<string.h>
int main() 
{	
   char p[10],q[10];
   int i,n;
   scanf("%s",p);
   n=strlen(a);
   for(i=0;i<n;i=i+2)
   {
       q[i]=p[i];
       printf("%c",q[i]);
   }
   printf(" ");
   for(i=1;i<n;i=i+2)
   {
       q[i]=p[i];
       printf("%c",b[i]);
   }
 return 0;
}
